Options -MultiViews
RewriteEngine On

# Send all requests to the SPA entrypoint except public assets
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
